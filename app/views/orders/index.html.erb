<h1>Lista de Ordenes </h1>

<table>
  <tr>
    <th>Nombre</th>
    <th>Empresa</th>
    <th>Teléfono</th>
    <th style="text-align:center;width:150px;">Fecha del pedido</th>
    <th></th>
    <th></th>
  </tr>

<% @orders.each do |order| %>
  <tr>
    <td><%= order.nombre %></td>
    <td><%= order.empresa %></td>
    <td><%= order.telefono %></td>
    <td style="text-align:center;width:150px;"><%= order.created_at %></td>
    <td><%= link_to 'Mostrar', order %></td>
    <td><%= link_to 'Eliminar', order, confirm: 'Esta seguro?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Order', new_order_path %>
