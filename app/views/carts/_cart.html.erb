<h2>Tu carro de compras</h2>
<table>
  <%= render(cart.line_items) %>
<tr class="total_line">
  <td colspan="2">Total</td>
  <td class="total_cell"><%= number_to_currency(cart.total_price, :unit => "Bsf ") %></td>
</tr>
</table>  
<%= button_to 'Chequear', new_order_path, method: :get, :class => 'button' %>
<%= button_to 'Vaciar carro', cart, method: :delete, remote: true , :class => 'button'%>