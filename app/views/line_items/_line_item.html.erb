<% if line_item == @current_item %>
<tr id="current_item">
<% else %>
<tr>
    <% end %>

 <% if @mailer %>
    <% @impuesto=((line_item.total_price.to_f)*12/100.to_f).round(2) %>
    <% @TotalItem=number_to_currency((line_item.total_price + @impuesto).round(2),:unit => "Bsf ") %>
    <td><%= line_item.producto.codigo %></td>
    <td><%= line_item.producto.modelo %></td>
    <td><%= line_item.producto.nombre %></td>
    <td><%= line_item.quantity %></td>
    <td><%= number_to_currency(line_item.producto.precio,:unit => "Bsf ") %></td>
    <td></td>
    <td></td>
    <td>12.0</td>
    <td><%= @impuesto %></td>
    <td><%= @TotalItem %></td>
    </tr>
<% else %>
    <td><%= line_item.quantity %>&times;</td>
    <td><%= line_item.producto.nombre %></td>
    <td class="item_price"><%= number_to_currency(line_item.total_price, :unit => "Bsf ")    %></td>
    </tr>
<% end %>