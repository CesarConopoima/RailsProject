<% if line_item == @current_item %>
<tr>
    <td id="current_item" class="item_price"><%= line_item.quantity %></td>
	<td class="item_price"><%= line_item.producto.nombre %></td>
    <td id="current_item" class="item_price"><%= number_to_currency(line_item.total_price, :unit => "Bsf ") %></td>
    <td><%= button_to "+", increase_line_item_path(line_item), :method => :put, remote: true %></td>
    <td><%= button_to "-", decrease_line_item_path(line_item), :method => :put, remote: true %></td>
<% else %>
<tr>
	<td class="item_price"><%= line_item.quantity %></td>
	<td class="item_price"><%= line_item.producto.nombre %></td>
    <td class="item_price"><%= number_to_currency(line_item.total_price, :unit => "Bsf ") %></td>
    <td><%= button_to "+", increase_line_item_path(line_item), :method => :put, remote: true %></td>
    <td><%= button_to "-", decrease_line_item_path(line_item), :method => :put, remote: true %></td>
<% end %>
</tr>